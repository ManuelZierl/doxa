pred social/1.
pred step/2.
pred req/2.
pred fam/1.
pred h/1.
social(s1).
social(s2).
step(s1, gov).
req(s1, r1).
req(s2, r2).
fam(r1).
fam(r2).
h(S) :- req(S, R), fam(R).
?- social(S), h(S), not step(S, gov).
?- h(S), not step(S, gov).
/- exit
