pred social/1.
pred step/2.
pred req/2.
pred fam/1.
pred pair/2.
social(s1).
step(s1, gov).
req(s1, r1).
fam(r1).
pair(S, R) :- req(S, R), fam(R).
?- pair(S, R).
?- social(S), pair(S, R).
?- social(S), pair(S, R), step(S, gov).
/- exit
