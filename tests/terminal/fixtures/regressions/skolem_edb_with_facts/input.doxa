pred social/1.
pred step/2.
pred req/2.
pred fam/1.
pred h/1.
social(s1).
step(s1, gov).
req(s1, r1).
fam(r1).
h(S) :- req(S, R), fam(R).
?- social(S), h(S).
?- social(S), h(S), step(S, gov).
?- h(S), step(S, gov).
/- exit
