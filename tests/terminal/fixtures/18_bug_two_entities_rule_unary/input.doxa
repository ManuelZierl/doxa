pred social/1.
pred step/2.
pred req/2.
pred fam/1.
pred h/1.
social(s1).
social(s2).
step(s1, gov).
step(s2, gov).
req(s1, r1).
req(s2, r2).
fam(r1).
fam(r2).
h(S) :- req(S, R), fam(R).
?- h(S).
?- social(S), h(S).
?- social(S), h(S), step(S, gov).
/- exit
