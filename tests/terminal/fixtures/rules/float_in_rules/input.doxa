pred price/2.
pred expensive/1.
pred cheap/1.
pred taxed/2.
price(apple, 1.5).
price(banana, 0.75).
price(cherry, 3.0).
expensive(X) :- price(X, P), gt(P, 1.0).
cheap(X) :- price(X, P), leq(P, 1.0).
taxed(X, T) :- price(X, P), mul(P, 1.2, T).
?- expensive(X).
?- cheap(X).
?- taxed(X, T).
/- exit
